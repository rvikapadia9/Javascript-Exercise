// Real QA Scenario (Automation Style)
// let retryCount = 0;


// Exercise:

// Increase retry count

// If retryCount exceeds 3, reduce it by 1

// Print final value

// (Use ++ and -= only)
let retryCount = 3;

// Increase retry count
retryCount++;

// If retryCount exceeds 3, reduce it by 1
if (retryCount > 3) {
    retryCount -= 1;
}

// Print final value
console.log(retryCount);
