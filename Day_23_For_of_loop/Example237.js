// Compare for vs for…of (Interview Favorite)
// let arr = ["a", "b", "c"];

// Exercise:
// Print values using for…of and explain why index is not available.

let arr = ["a", "b", "c"];
for(let value of arr){
    console.log(value);
}
console.log("----------------");
// for loop
for(let i = 0; i < arr.length; i++){
    console.log(arr[i]);
}

//Explanation:
// The for...of loop is used to iterate over the elements of an array.
// The loop iterates over the elements of the array in order.
// The loop terminates when all elements of the array have been iterated over.

// Why index is not available in for...of loop:
// The for...of loop is used to iterate over the elements of an array.
// The loop iterates over the elements of the array in order.
// The loop terminates when all elements of the array have been iterated over.

